name: inventory_tracker

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # ✅ SQLite
  sqflite: ^2.3.0
  path: ^1.9.0
  mobile_scanner: ^7.0.1
  csv: ^5.0.2
  file_picker: ^6.1.1

flutter:
  uses-material-design: true
